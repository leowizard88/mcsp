---
layout: base.njk
title: MANCUSPIE
---
<style>
  @import url('https://fonts.googleapis.com/css2?family=Gaegu:wght@300;400&display=swap');

  .hero-panel::after { display: none !important; }

  .hero-copy .child-title {
    font-family: 'Gaegu', 'Comic Sans MS', cursive;
    font-size: clamp(82px, 13vw, 192px);
    line-height: .76;
    letter-spacing: -.065em;
    font-weight: 400;
    margin: 0 0 22px;
    color: transparent;
    text-transform: uppercase;
    background-image:
      repeating-linear-gradient(103deg, rgba(10,9,8,.98) 0 1px, rgba(10,9,8,.82) 1px 2px, rgba(10,9,8,.56) 2px 3px, transparent 3px 5px),
      repeating-linear-gradient(17deg, rgba(10,9,8,.74) 0 1px, rgba(10,9,8,.30) 1px 2px, transparent 2px 4px),
      radial-gradient(circle at 23% 37%, rgba(10,9,8,.82), transparent 2px),
      radial-gradient(circle at 61% 64%, rgba(10,9,8,.72), transparent 2px);
    background-size: 13px 100%, 23px 100%, 11px 9px, 17px 15px;
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    -webkit-text-stroke: .18px rgba(10,9,8,.52);
    text-shadow:
      .24px .10px 0 rgba(10,9,8,.42),
      -.18px .20px 0 rgba(10,9,8,.24),
      .06px -.14px 0 rgba(10,9,8,.18);
    filter: contrast(1.34) grayscale(.35);
  }

  .child-title span {
    display: inline-block;
    transform-origin: 50% 70%;
    opacity: 1;
  }

  .child-title .l1 { transform: rotate(-5deg) translateY(4px); }
  .child-title .l2 { transform: rotate(3deg) translateY(-7px); opacity:.96; }
  .child-title .l3 { transform: scaleX(-1) rotate(-7deg) translateY(8px); margin: 0 .015em; opacity:.98; }
  .child-title .l4 { transform: rotate(6deg) translateY(-2px); opacity:.97; }
  .child-title .l5 { transform: rotate(-3deg) translateY(6px); opacity:.95; }
  .child-title .l6 { transform: rotate(4deg) translateY(-5px); opacity:1; }
  .child-title .l7 { transform: rotate(-6deg) translateY(4px); opacity:.96; }
  .child-title .l8 { transform: rotate(2deg) translateY(-3px); opacity:.94; }
  .child-title .l9 { transform: rotate(-4deg) translateY(5px); opacity:.98; }

  .ultimi-movimenti-button {
    position: absolute;
    right: clamp(14px, 4vw, 58px);
    bottom: clamp(14px, 5vw, 66px);
    z-index: 9;
    display: block;
    width: clamp(310px, 30vw, 510px) !important;
    opacity: .94;
    mix-blend-mode: multiply;
    transform-origin: 60% 72%;
  }
  .ultimi-movimenti-button img {
    display: block;
    width: 100%;
    height: auto;
    mix-blend-mode: multiply;
    filter: contrast(1.05) saturate(.82);
  }
  .ultimi-movimenti-button:hover {
    animation: movimentiVibra .115s steps(2, end) infinite;
    opacity: 1;
  }
  @keyframes movimentiVibra {
    0% { transform: translate(0, 0) rotate(-1deg); }
    25% { transform: translate(2px, -1px) rotate(1.2deg); }
    50% { transform: translate(-2px, 1px) rotate(-.8deg); }
    75% { transform: translate(1px, 2px) rotate(.7deg); }
    100% { transform: translate(-1px, 0) rotate(-1.1deg); }
  }
  @media(max-width:760px){
    .hero-copy .child-title{font-size:64px;letter-spacing:-.07em}
    .ultimi-movimenti-button{width:215px !important;right:10px;bottom:16px;opacity:.9}
  }
</style>

<section id="home" class="panel hero-panel">
  <div class="bg-layer bg-daisies"></div>
  <div class="bg-layer bg-mechanical"></div>
  <div class="bg-layer bg-score"></div>
  <div class="bg-layer bg-figure"></div>
  <div class="hero-copy">
    <p class="eyebrow">{{ home.hero_note }}</p>
    <h1 class="child-title" aria-label="MANCUSPIE"><span class="l1">M</span><span class="l2">A</span><span class="l3">N</span><span class="l4">C</span><span class="l5">U</span><span class="l6">S</span><span class="l7">P</span><span class="l8">I</span><span class="l9">E</span></h1>
    <div class="hairline"></div>
    <h2>{{ site.subtitle }}</h2>
    <p>{{ site.intro }}</p>
  </div>
  <a class="ultimi-movimenti-button" href="/ultimi-movimenti/" aria-label="Vai a Ultimi movimenti">
    <img alt="Figura accovacciata" src="/assets/img/adfe1ddc4e68f7828ffef4e2737ca5a2.jpg">
  </a>
</section>

<section id="call" class="panel content-panel call-panel">
  <div class="bg-layer bg-fog"></div>
  <article class="section-block wide">
    <p class="section-kicker">01</p>
    <h2>{{ home.call_title }}</h2>
    <p>{{ home.call_text }}</p>
    <p class="meta">Scadenza: {{ home.call_deadline }}</p>
    {% for call in collections.call | reverse %}
      {% if loop.index0 < 1 %}<a class="text-link" href="{{ call.url }}">Leggi il bando →</a>{% endif %}
    {% endfor %}
  </article>
</section>

<section id="testi" class="panel content-panel testi-panel">
  <div class="bg-layer bg-tree"></div>
  <article class="section-block list-block">
    <p class="section-kicker">02</p>
    <h2>Testi</h2>
    <div class="item-list">
      {% for post in collections.testi | reverse %}
      {% if loop.index0 < 4 %}
      <a class="item" href="{{ post.url }}">
        <span>{{ post.data.title }}</span>
        <em>{{ post.data.author }}</em>
      </a>
      {% endif %}
      {% endfor %}
    </div>
  </article>
</section>

<section id="note" class="panel content-panel note-panel">
  <div class="bg-layer bg-sigils"></div>
  <article class="section-block list-block offset-right">
    <p class="section-kicker">03</p>
    <h2>Note</h2>
    <div class="item-list">
      {% for post in collections.note | reverse %}
      {% if loop.index0 < 4 %}
      <a class="item" href="{{ post.url }}">
        <span>{{ post.data.title }}</span>
        <em>{{ post.data.author }}</em>
      </a>
      {% endif %}
      {% endfor %}
    </div>
  </article>
</section>

<section id="noi" class="panel content-panel noi-panel">
  <div class="bg-layer bg-archive"></div>
  <article class="section-block wide">
    <p class="section-kicker">04</p>
    <h2>Noi</h2>
    <p>{{ home.noi_text }}</p>
    <a class="text-link" href="/noi/">Chi siamo →</a>
  </article>
</section>

<section id="progetti" class="panel content-panel projects-panel">
  <div class="bg-layer bg-spine"></div>
  <article class="section-block list-block">
    <p class="section-kicker">05</p>
    <h2>Altri progetti</h2>
    <div class="item-list">
      {% for project in collections.progetti | reverse %}
      {% if loop.index0 < 4 %}
      <a class="item" href="{{ project.url }}">
        <span>{{ project.data.title }}</span>
        <em>{{ project.data.excerpt }}</em>
      </a>
      {% endif %}
      {% endfor %}
    </div>
  </article>
  <footer class="footer">
    <span>© MANCUSPIE</span>
    <span>{{ site.email }}</span>
    <span>{{ home.newsletter_text }}</span>
  </footer>
</section>
