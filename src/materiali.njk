---
layout: base.njk
title: Materiali vari | MANCUSPIE
permalink: /materiali/
---
<style>
  body:has(.materiali-page), body:has(.materiali-page) .site-main { background: #191915 !important; }
  .materiali-page {
    position: relative;
    display: grid;
    align-content: start;
    min-height: 100vh;
    box-sizing: border-box;
    overflow: hidden;
    padding: clamp(32px, 7vw, 104px);
    color: #fffdf4;
    background:
      linear-gradient(90deg, rgba(11, 13, 13, .76) 0%, rgba(11, 13, 13, .46) 43%, rgba(11, 13, 13, .12) 77%, rgba(11, 13, 13, .20) 100%),
      url('/assets/img/materiali-vari.svg') center / cover no-repeat;
  }
  .materiali-page::after {
    content: "";
    position: absolute;
    inset: 0;
    pointer-events: none;
    background: repeating-linear-gradient(0deg, rgba(255,255,255,.035) 0 1px, transparent 1px 5px);
    mix-blend-mode: overlay;
  }
  .materiali-inner { position: relative; z-index: 1; max-width: min(850px, 82vw); }
  .materiali-kicker { margin: 0 0 18px; font: 400 11px/1 var(--font-sans, system-ui, sans-serif); letter-spacing: .22em; text-transform: uppercase; }
  .materiali-title { margin: 0; font-family: var(--font-serif, Georgia, serif); font-size: clamp(68px, 13.8vw, 176px); line-height: .76; font-weight: 300; letter-spacing: -.055em; text-transform: uppercase; text-wrap: balance; }
  .materiali-rule { width: min(360px, 72vw); height: 1px; margin: clamp(24px, 4vw, 45px) 0 18px; background: rgba(255,253,244,.76); }
  .materiali-subtitle { max-width: 560px; margin: 0; font: 300 clamp(20px, 2.4vw, 34px)/1.1 var(--font-serif, Georgia, serif); }
  .materiali-back { display: inline-block; margin-top: clamp(36px, 7vw, 84px); color: inherit; border-bottom: 1px solid currentColor; padding-bottom: 7px; font: 400 11px/1 var(--font-sans, system-ui, sans-serif); letter-spacing: .16em; text-transform: uppercase; text-decoration: none; }
  @media (max-width: 700px) {
    .materiali-page { padding: 30px 24px; background-position: 58% center; }
    .materiali-inner { max-width: min(510px, 91vw); }
    .materiali-title { font-size: clamp(60px, 18vw, 100px); }
    .materiali-subtitle { font-size: 22px; }
  }
</style>
<section class="materiali-page panel">
  <div class="materiali-inner">
    <p class="materiali-kicker">Altri progetti</p>
    <h1 class="materiali-title">Materiali vari</h1>
    <div class="materiali-rule"></div>
    <p class="materiali-subtitle">Cose che ci piacciono o che vogliamo mettere a disposizione del mondo.</p>
    <a class="materiali-back" href="/progetti/">Altri progetti</a>
  </div>
</section>
